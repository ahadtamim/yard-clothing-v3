import React from 'react'

// We define a blank interface so TypeScript has nothing to complain about
export const ArchiveBlock: React.FC<any> = () => {
  return null // This block won't render anything, which is fine for your e-com store
}

export default ArchiveBlock